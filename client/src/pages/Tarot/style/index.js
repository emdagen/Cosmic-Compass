import { keyframes } from 'styled-components';

export const tarotButtonProps = {
  fullWidth: true,
  size: 'large',
  sx: { mt: 2 },
  variant: 'contained',
};

export const breatheAnimation = keyframes`
 0% { opacity: 0; }
50% { opacity: 1; }
 0% { opacity: 0; }
`;
